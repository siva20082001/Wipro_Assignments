// Import React
import React from "react";

// Define a functional component called ErrorComponent
const ErrorComponent = () => {
  // Return JSX showing an error message with a warning emoji
  return <p>⚠️ Error fetching product details</p>;
};

// Export the component
export default ErrorComponent;
