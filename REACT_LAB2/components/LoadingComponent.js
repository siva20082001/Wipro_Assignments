// Import React (required to define a React component)
import React from "react";

// Define a functional component called LoadingComponent
const LoadingComponent = () => {
  // Return JSX that displays a loading message
  return <p>Loading product details...</p>;
};

// Export the component for use in Task17.js
export default LoadingComponent;
